
- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br/
		%span
			Share your dog with the world and make someones day!
- else
	%p#intro
		Dog photos that make you smile
		%br/
		%span
			Log-in and post a photo of your puppy!!!
#posts
	- @posts.each do |post|
		.post[@post]
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.name
					%p.buttons
						%span
						= link_to like_post_path(post), method: :get,class: "data" do
							%i.fa.fa-thumbs-o-up
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size
