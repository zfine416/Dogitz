- if user_signed_in?
	%p#intro
		Hello
		= current_user.name
		%br/
		%span
			The Cutest Dogs of All-Time!!!
		.sort
			See the cutest dogs from 
			%span{:onclick => "dropdown()"}
				All Time
				%i.fa.fa-caret-down
			%ul
				%li
					= link_to "Today",  root_path
				%li
					= link_to "This Week",  posts_week_path
				%li
					= link_to "This Month",  posts_month_path
- else
	%p#intro
		Dog photos that make you smile
		%br/
		%span
			Log-in and post a photo of your puppy!!!
#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size